{{#if items}}
<ul>
  {{#each items}}
  <li>
    <a href="/question/{{id}}">{{content}}</a>
    <ol>
      {{#each answers as |answer|}}
      <li>{{answer}}</li>
      {{/each}}
    </ol>
  </li>

  {{/each}}
</ul>
{{/if}}

<a href="/question/new" class="btn btn-success">Ask a new question</a>